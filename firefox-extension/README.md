# Otosu Firefox拡張機能

## 一時的にインストールして試す

1. Firefoxで `about:debugging#/runtime/this-firefox` を開く
2. 「一時的なアドオンを読み込む」を選ぶ
3. このフォルダーの `manifest.json` を選ぶ
4. YouTubeを開き、ツールバーのOtosuボタンまたは右クリックメニューを使う

Otosuを一度起動して `otosu://` プロトコルを登録してから使用してください。
